# STATUS.md — สถานะโปรเจกต์ + จุดเริ่ม session

> 📍 **เปิดไฟล์นี้เป็นไฟล์แรกของทุก session**
> อัปเดตล่าสุด: **2026-08-24**
> ✅ **L1: ลบบัญชีทดสอบค้าง `mju6606105386@mju.ac.th` แล้ว** (pete ยืนยัน 2026-08-24) — สมัครไม่สำเร็จตั้งแต่ 2026-08-13 (ไม่เคยยืนยันอีเมล ไม่มี `"Profile"`) ลบตรงจาก `auth.users` (cascade ไป `auth.identities` อัตโนมัติ) ปิดคิว D-32 ข้อ 1 ที่เหลือค้างอยู่ · **หนี้ L1 ที่เหลืออีก 2 ข้อ (D-34) — pete ตัดสินใจแล้ว: ยังไม่ verify domain ที่ Resend (ยังไม่มีโดเมนพร้อม, คง manual relay ต่อ), คง rate limit 30/ชม. ต่อไป** ไม่มีอะไรให้ลงมือทำเพิ่มตอนนี้
> ✅ **D-65: P-09 (รองรับรีพอร์ต "ผู้ใช้") เริ่มและปิดครบ** — `reports.reported_user_id` + 2 CHECK ใหม่ (ต้องมีเป้าหมายอย่างน้อย 1 อย่าง, กันรีพอร์ตตัวเอง) + unique partial index มิเรอร์ของเดิม `reports_admin_view` เพิ่ม 4 คอลัมน์ (`reported_user_id`/`reported_user_name`/`is_product_report`/`is_user_report`) — **ปิดช่องโหว่ crash แฝงจาก D-24 ไปด้วย**: `ReportDetailContent` เดิม force-unwrap `product_title!`/`seller_name!` แบบไม่มีเงื่อนไข (NULL ได้จริงเมื่อสินค้าที่ถูกรายงานโดนลบทีหลัง) ตอนนี้ครอบด้วย `if (isProductReport==true)`/`if (isUserReport==true)` แล้วทั้งคู่ FlutterFlow 2 พุช: `ReportUserSheet` (mirror `ReportProductSheet`) + ปุ่ม "รายงานผู้ใช้" ใน `UserProfileCard` (ครอบคลุมทุกจุดเข้าโปรไฟล์อัตโนมัติ) ผูก visibility ซ่อนตอนดูโปรไฟล์ตัวเองด้วย typed `visible: Not(Equals(...))` ล้วน (ไม่ต้อง raw proto ต่างจาก D-51 เพราะทั้งสองฝั่งเป็น typed expression) + re-author `ReportDetailContent` แยกโชว์บล็อกสินค้า/ผู้ใช้ + ปุ่ม "ระงับผู้ใช้รายนี้" ใหม่ (reuse `banUserSheetRef` เดิม) ยืนยันจาก `generated_code/` รายละเอียด `DECISIONS.md` D-65 — **ยังไม่ได้ทดสอบผ่านแอปจริง**
> ✅ **D-64: Layer 7 `reviews` (ให้คะแนนผู้ขาย) เริ่มและปิดครบ ปิดข้อเสนอ P-08** — ตาราง `reviews` ผูกกับ `transactions` (L5) โดยตรงแทน `product_id` ตรง ๆ ตาม draft เดิม (`UNIQUE(transaction_id, reviewer_id)`, RLS insert เช็ค `EXISTS` เทียบ `transactions` ว่าเป็นผู้ซื้อจริง) — public read, immutable ตลอดไป (ไม่มี UPDATE/DELETE policy เลย), P-09 (รีพอร์ตผู้ใช้) แยกไปทำทีหลัง ตามคำตอบ pete `products_review_view` เพิ่ม 4 คอลัมน์คอมพิวต์ (`seller_avg_rating`/`seller_review_count`/`my_transaction_id`/`can_rate_seller`) แทนสร้าง view ใหม่ ("ง่ายที่สุดจากสถาปัตยกรรมตอนนี้") FlutterFlow 2 พุช: component `RateSellerSheet` (`Slider` 1-5 แทน star rating จริง — DSL ไม่มี `RatingBar` ที่ construct ได้) + custom action `submitSellerReview` (0-arg, หา `transaction_id`/`seller_id` เองแล้ว insert ตรง ไม่ผ่าน RPC) + ปุ่ม "ให้คะแนนผู้ขาย" บน `ProductDetails` ผูก visibility เข้ากับ `can_rate_seller` (พุชแยกตาม PT-22) 🔴 เจอกับดักใหม่: `UpdateAppState.set` target ต้องเป็น string/typed handle ห้ามห่อด้วย `AppState(...)` (คลาสสำหรับอ่านค่า ไม่ใช่ target) ยืนยันจาก `generated_code/` รายละเอียด `DECISIONS.md` D-64 — **ยังไม่ได้ทดสอบผ่านแอปจริง**
> ✅ **D-61/D-62/D-63: L2/L3 (Product Listings + Browse/Search) — `category_id` NOT NULL, substring search RPC จริงครั้งแรก, ช่วงราคา** — **D-61**: `products.category_id` เป็น `NOT NULL` แล้ว (0 แถว null ตอน apply) ไม่เปิด browse ก่อน login/ไม่ทำ `"CAT"` admin CRUD UI (pete ตอบตรง ๆ) **D-62**: RPC `search_products(keyword, category_id, min_price, max_price)` ปิดข้อเสนอ P-05 — ค้นหา substring จริงทั้ง title/description แทน exact-match เดิม (D-46/D-48 สรุปไว้ว่า `iLike` ไม่มี null-safe codegen) เรียกผ่าน custom action 0-arg (`CallCustomAction` ส่ง argument ไม่ได้เลย, PT-09) **ค้นพบสำคัญ**: `SetState` บน `List<PostgresRow>` field รับค่าจาก custom **ACTION**'s `ActionOutput` ได้จริง (ต่างจาก custom FUNCTION ที่ D-45 เจอว่าใช้ไม่ได้) เปิดทาง RPC-backed list เต็มรูปแบบโดยไม่ต้องแก้ ListView เดิม — onLoad/search/ปุ่มค้นหา/pull-to-refresh/category chip ทั้ง 13 หมวดเปลี่ยนมาเรียก RPC เดียวกันหมด **D-63**: ช่วงราคาเป็นแกนอิสระ (ไม่ล้าง/ไม่ถูกล้างโดย keyword/category) — ใช้ String app state + parse เองด้วย `double.tryParse` แทนพึ่ง DSL coerce `WidgetState(text)→Double` (ไม่เคยพิสูจน์ปลอดภัยมาก่อน) ยืนยันครบทุกจุดจาก `generated_code/` รายละเอียด `DECISIONS.md` D-61/D-62/D-63, pattern ใหม่ `PATTERNS.md` PT-33 — **ยังไม่ได้ทดสอบผ่านแอปจริง**
> ✅ **D-60: Realtime (chat + Notifications) เปิดจริงครั้งแรก** — ปิดหนี้ D-32 บางส่วน (unread/live-update ฝั่ง chat+notifications; ยังเหลือ badge ตัวเลข/P-04 ที่ตัดสโคปไว้ตามที่ pete สั่ง) กลไก: page-level `databaseRequest` (raw proto) + `isStreamingSupabaseQuery: true` + `ON_DATA_CHANGE` trigger รัน onLoad query+SetState เดิมซ้ำ (ต่างจาก action-based `PostgresQuery`'s เดียวกัน flag ซึ่งลองแล้วไม่ทำงาน) ยืนยันจาก `generated_code/` ว่า compile เป็น `StreamBuilder`+`SupaFlow.client...stream(...)` จริงทั้ง 3 หน้า (`Notifications`/`chatList`/`chatMessages`) Realtime target เป็น base table เสมอ (`chat_message`/`notifications`) ไม่ใช่ view (D-29) **+ ปิดหนี้พ่วง**: `findOrCreateChatWithSeller` (ปุ่ม "แชทกับผู้ขาย") ตอนนี้ relay `memberNames`/`userIds` ผ่าน `chat_summary` แล้ว (เดิมส่งแค่ `chatId`, L4-chat.md ข้อ 3) รายละเอียด `DECISIONS.md` D-60, pattern ใหม่ `PATTERNS.md` PT-32 — **ยังไม่ได้ทดสอบผ่านแอปจริง** (แผนคือใช้บัญชีเพื่อน pete จริง 2 บัญชี แล้วลบทิ้งหลังทดสอบ)
> ✅ **D-59: Layer 5 (Transaction & Listing Status) เริ่มและปิดครบ + ปิดหนี้ D-03** — "ปิดการขาย" เริ่มจาก `chatMessages` (เลือกประกาศตัวเองขายให้คู่สนทนา ไม่ใช่เลือกผู้ซื้อ — chat ไม่ผูก product_id) ผ่าน RPC `mark_product_sold` (SECURITY DEFINER, กัน race ด้วย PT-05, เขียน `transactions` ในฟังก์ชันเดียวกันเลี่ยง select-back) `products.status` เข็มแข็งขึ้น (NOT NULL + CHECK) + `buyer_id`, ตาราง `transactions` ใหม่ query ได้จริง (RLS อ่านได้เฉพาะคู่ค้า/แอดมิน เขียนได้ทาง RPC เท่านั้น), `admin_sales_by_seller` repoint ไปอ้าง `transactions` แทน `products.status='sold'` ตรง ๆ (`HomeAdmin` ไม่ต้องแก้ DSL เลย) **+ ปิดหนี้ D-03 ไปพร้อมกัน** (`products` allow-all → 4 policy ตาม cmd จริง + trigger ใหม่ `enforce_sale_via_rpc_only` ล็อก `status`/`buyer_id` ให้ผ่าน RPC เท่านั้นแม้เจ้าของ/แอดมินก็แก้ตรงไม่ได้) `Home`/`Mypost`/`ProductDetails` ผูกครบ (ของหายจาก grid เมื่อขายแล้ว, badge บน `Mypost`, ผู้ซื้อโชว์เฉพาะเจ้าของ/แอดมินบน `ProductDetails`) ยืนยันด้วย impersonation test จริงครบทุกจุด (RPC 3 เส้นทาง, RLS overhaul 3 บทบาท) + เช็ค `generated_code/` ทุกพุช รายละเอียด `DECISIONS.md` D-59 — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-58: Admin advertisement posts + likes บน `Home` (`ListView_6etuspo6`)** — เดิมเป็น static placeholder ทิ้งจาก template เพิ่มตาราง `advertisement_posts`/`advertisement_likes` (junction, per-user like จริง ไม่ใช่ counter — pete เลือกแบบนี้) + view `advertisement_posts_view` (like_count/liked_by_me) `Home` rebind โชว์โพสต์ใหม่สุดก่อน พร้อมปุ่มไลก์/เลิกไลก์ (refetch เต็มหลังกด ไม่ optimistic) หน้าใหม่ `AdminCreatePost` (admin gate ผ่าน `IsCurrentUserAdmin` เดิม, อัปโหลดรูปเข้า bucket ใหม่ `ad-post-images`, ฟอร์มหัวข้อ/รายละเอียด optional) + ทางเข้าใหม่บน `HomeAdmin` side-nav แยก 4 พุชตาม PT-15/30 (register ตาราง+rebind Home → ensurePage shell → body จริง+gate+nav → raw-proto patch bucket/URL capture) 🔴 เจอบั๊กเอง 1 จุดก่อนส่งมอบ (capture URL ผูกผิด field เพราะเดา key จาก grep ไม่ได้อ่านไฟล์ตรง ๆ) แก้แล้วด้วยพุชเดี่ยว ยืนยันจาก `generated_code/` รายละเอียด `DECISIONS.md` D-58, RLS ยืนยันด้วย impersonation test จริงแล้ว (ผ่าน SQL) — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-57: `ProfileUser` เพิ่ม Dropdown ชั้นปี/คณะ ให้ผู้ใช้กรอกข้อมูลตัวเองได้จริง** — `RenameProfileSection` เดิม (มีแค่ช่องแก้ชื่อ) เพิ่ม 2 `Dropdown` (ชั้นปี 1-4, คณะ 4 ตัวเลือกตรงกับตาราง `faculties`) แต่ละฟิลด์ (ชื่อ/ชั้นปี/คณะ) เซฟแบบ optional อิสระ ("เว้นว่างไว้เท่าเดิม" ตามที่ pete สั่ง — ไม่ใช่ update ก้อนเดียวที่จะเผลอเขียน NULL ทับของเดิม) ข้อมูลที่บันทึกไหลไปโชว์ใน `UserProfileCard` (D-55) อัตโนมัติทันทีเพราะ chain ต่อกันอยู่แล้ว (`Profile` → `public_profiles`/D-56 join `faculties` → `loadViewedProfile`/D-55) ไม่ต้องแก้อะไรเพิ่ม 🔴 เจอ 2 กับดักใหม่สำคัญ: `ff.Tables.profile` typed field ค้างมาตั้งแต่ก่อน D-52 ไม่มี `year_of_study`/`faculty_id` เลยทั้งที่คอลัมน์มีจริง ต้อง `addTableField` เพิ่ม, และ**ค้นพบ limit ใหม่ของ SDK: widget เดียวมี action ที่ผลิต output variable ได้สูงสุด 5 ตัว** (ยืนยันเชิงประจักษ์ด้วยการสลับลำดับ branch) เกินแล้ว collide เสมอไม่ว่าจะตั้งชื่อ unique แค่ไหน — แก้ด้วยการย้าย string→int conversion ไปทำที่ Dropdown's onChanged (SetState ล้วน ไม่ติด limit) รายละเอียด `DECISIONS.md` D-57, pattern ใหม่ `PATTERNS.md` PT-31 (และแก้ไข root cause ของ PT-30 §2 ย้อนหลังด้วย) — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-56: หน้า "ผู้ใช้งานทั้งหมด" (`UserDirectory`) + ตาราง `faculties` แยก** — เข้าจาก header ของ `Home` (ไอคอนใหม่ต่อจาก `NotificationsBellButton`) list ผู้ใช้ทุกคน**ยกเว้นคนถูกแบน** (view ใหม่แยก `public_directory_view`, `WHERE NOT is_banned` — ไม่แตะ `public_profiles` เดิมเพราะ `UserProfileCard`/D-55 ต้องใช้เปิดโปรไฟล์คนถูกแบนจากหน้า admin ได้ต่อ) กรองชั้นปี (1-4, CHECK แคบลงจาก 1-8 เดิม)/คณะ (4 คณะ placeholder "คณะ A-D" รอ pete แก้ชื่อจริง) แบบ mutually-exclusive (แตะแกนไหนล้างอีก 2 แกน — proven pattern จาก `Home` category chip ไม่ใช่ AND รวมกัน เพราะ SDK นี้ทำ dynamic filter list ไม่ได้) + ค้นหาชื่อ exact match (ข้อจำกัดเดิมจาก D-48) แถวเป็น `ListView`+หัวตาราง static (ไม่ใช่ `PaginatedDataTable` จริง — เช็คแล้วพบว่า DSL bind ได้แค่หัวคอลัมน์ ไม่มี itemBuilder ให้เลย) แตะแถวเปิด `UserProfileCard` เดิมจาก D-55 ผ่าน `openProfileChain` ที่ reuse ตรง ๆ `Profile.faculty` (text, D-55) ย้ายเป็น `faculty_id` FK ชี้ `faculties` แทน — `public_profiles`/`UserProfileCard` ไม่กระทบเลยเพราะผลลัพธ์คอลัมน์เหมือนเดิม 🔴 เจอ 2 กับดักใหม่: table/view ที่สร้างตรงใน Supabase ต้อง register ผ่าน `postgres_helpers.addTable` ก่อน DSL อ้างถึงได้ (`refresh-context` เฉย ๆ ไม่พอ), outputAs ที่ unique ตามกฎ D-39 แล้วก็ยัง collide ได้ต้องเปลี่ยนรูปแบบชื่อทั้งชุด (root cause ไม่ทราบแน่ชัด) รายละเอียด `DECISIONS.md` D-56, pattern ใหม่ `PATTERNS.md` PT-30 — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-55: ป๊อปอัพโปรไฟล์ผู้ใช้ (avatar/ชื่อ/bio/ชั้นปี/คณะ) จาก `ProductDetails` + `BannedUsers`** — เพิ่มคอลัมน์ `"Profile".year_of_study`/`faculty` (nullable) แล้วขยาย `public_profiles` (D-01, exposure tier เดียวกับ `full_name`/`avatar_url` เดิม ไม่ใช่การเปิดเผยข้อมูลใหม่) แตะชื่อผู้ขาย/ชื่อผู้ใช้เปิด component `UserProfileCard` (paramless, อ่าน App State `viewedProfile*`) ผ่าน custom action `loadViewedProfile` — reset ทุก field ก่อน query ทุกครั้งกัน state leak ข้ามโปรไฟล์ + เปิดการ์ดเฉพาะตอน query สำเร็จ ชั้นปี/คณะโชว์ "ยังไม่ระบุ..." แทนค่าว่างเปล่า (ข้อมูลจริงตอนนี้ว่างเกือบทุกคนเพราะยังไม่มีฟีเจอร์กรอกข้อมูล) ทั้ง 2 entry point ต้อง re-author itemBuilder ทั้งก้อนแบบ verbatim (PT-23 §1) ยืนยันด้วย diff กับไฟล์ backup ว่าไม่มีฟังก์ชันเดิมหาย (ปุ่ม Unban/ปุ่มแชท/ราคา/บรรยายสินค้า ฯลฯ ครบ) 🔴 เจอ 2 กับดักใหม่: `ensureReplaced` รันซ้ำด้วย key ที่เพิ่ง landed ไปแล้วพังทั้งโปรเจกต์ (ต้อง `inspect --outline` สดก่อน retry เสมอ), ห่อ `ListView` เป้าหมายของ `ensureReplaced` ด้วย `Expanded` ก็พังเหมือนกัน (ใช้ `shrinkWrap: true` แทน) รายละเอียด `DECISIONS.md` D-55, pattern ใหม่ `PATTERNS.md` PT-16 ข้อ 3, ข้อยกเว้นสำคัญที่แก้ไข PT-25 §1 (`UpdateAppState.set` รับ `item['x']` ได้ ต่างจาก `SetState`/`Navigate.params`) — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-53: หน้า `BannedUsers` แยกจาก `ManageUsers` แล้ว (2 พุช)** — pete ขอหน้าแยกเฉพาะผู้ใช้ที่ถูกแบน (ไม่ใช่ filter บน `ManageUsers` เดิม) โชว์ `banned_at` ("ถูกแบนเมื่อ") แทน duration/expiry เพราะระบบ ban เป็นถาวรอย่างเดียว (D-52) ไม่มีคอลัมน์นั้นจริง ListView กรอง `is_banned = true` + ปุ่ม Unban เดียว (`visible: can_unban`, เรียก `adminUnbanUser` ตัวเดิม) + ปุ่มรีเฟรชระดับหน้า + ปลุก tap การ์ดสถิติ "ผู้ใช้ถูกระงับ" บน `HomeAdmin` ให้ Navigate มาหน้านี้ (ยืนยันแล้วว่าตัวเลขบนการ์ดผูกกับ `admin_dashboard_stats.banned_users` จริงมาตั้งแต่ D-52 ไม่ใช่ placeholder) ยืนยันจาก `generated_code/` รายละเอียด `DECISIONS.md` D-53 — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-52: ระบบ Ban User เสร็จครบทั้ง Supabase + UI (Phase A–D, 8 พุช)** — `is_banned` มีคอลัมน์มาตั้งแต่ 2026-08-14 แต่ไม่เคยมี enforcement เลย ปิดหนี้นี้แล้วด้วย **RESTRICTIVE policy 5 ตัว** (ตัวแรกของโปรเจกต์ — PERMISSIVE ใช้ไม่ได้เพราะ allow-all ของ `products`) + trigger กันปลดแบนตัวเอง (ช่องโหว่จริงที่เจอ: `with_check` เดิมล็อกแค่ `role`/`student_id`) + RPC `admin_set_user_ban` เดียวจบ (เลี่ยง select-back D-24) + gate-in-view ซ่อนประกาศผู้ถูกแบนโดยไม่ต้องแก้ query ฝั่ง FF เลย ฝั่ง UI ได้หน้า `ManageUsers`/`BanUserSheet`/popup แจ้งเตือนบน `Home` (เปิดช่องอุทธรณ์ไว้ แชทแอดมินได้จริง) ยืนยันจาก `generated_code/` + impersonation test 3 บทบาท + `dart analyze` สะอาด รายละเอียด `DECISIONS.md` D-52, pattern ใหม่ `PATTERNS.md` PT-28/PT-29 — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**

> ✅ **D-51: `ProductDetails` ซ่อนปุ่ม "รายงาน"/"แชทกับผู้ขาย" จากเจ้าของประกาศเอง** — ปิดหนี้ D-32 ข้อ 4 ที่เข้าใจผิดว่าติด ItemRef/generator scope — ตัวจริงทั้งสองปุ่ม (`IconButton_k689spgx`/`Button_p7zb7whe`) เป็น page-level sibling ธรรมดา ไม่ได้อยู่ใน itemBuilder เลย บล็อกจริงคือไม่เคยมี `seller_id` ระดับหน้าให้ผูกมาก่อน D-44 ต่อยอด page-scoped query เดิมของ D-44 (รูปที่ 2/3) hand-replicate `Not(Equals(...))` shape ผ่าน raw proto (typed `Equals()`/`Not()` รับแค่ State/Param/AuthUser ไม่รับ raw page-scoped FFVariable) ยืนยันจาก `generated_code/`: คอมไพล์เป็น `Visibility`/`if` conditional จริง ไม่ใช่ static ที่เคยพังมาก่อน รายละเอียด `DECISIONS.md` D-51 — **ยังไม่ได้ทดสอบผ่านแอปจริง**
> ✅ **D-50: `Home` เพิ่มปุ่ม "ค้นหา" ให้กด submit ได้จริง** — pete รายงานว่าช่องค้นหาไม่มีปุ่ม submit เลย (เดิมยิง query แค่ตอนกด IME "search"/"done" บนคีย์บอร์ด) เพิ่ม `Button` "ค้นหา" (`Button_ym795py5`, ชื่อ `SearchSubmitButton`) เป็น sibling ต่อจากช่องค้นหาเดิม ไม่แตะช่องเดิมเลย เรียก query chain เดียวกับปุ่ม submit เดิม (`buildSearchRefreshChain`) คนละ `outputAs` กันชน ยืนยันจาก `generated_code/` รายละเอียด `DECISIONS.md` D-50 — **ยังไม่ได้ทดสอบผ่านแอปจริง**
> ✅ **D-49: จุดแดง unread stale-state (D-32) แก้ครบ 3 หน้าแล้ว** — พบว่า `chatList` แก้ไปแล้วจริงตั้งแต่ก่อนหน้านี้ (เอกสารตกหล่นไม่ได้อัปเดตตาม) `Notifications`/`ReportsFeedback` ยังไม่เคยแก้จริง เพิ่ม refetch+SetState ต่อท้าย ON_TAP chain เดิมทั้งคู่ (เทคนิคเดียวกับ `chatList`) ระหว่างทางเจอ `app.removeCustomFunction('wrapSearchPattern')` (D-48) ที่สำเร็จแล้วแต่ไม่ได้ retire ออกจากสคริปต์ ทำให้ push นี้ค้าง `CustomCodeNotFoundError` — ลบบรรทัดออกแล้ว push ผ่าน ยืนยันจาก `generated_code/` ครบ 3 หน้า รายละเอียด `DECISIONS.md` D-49 — **ยังไม่ได้ทดสอบผ่านแอปจริงโดย pete**
> 🔴 **D-48: `Home` ค้นหาถอยกลับเป็น exact match — substring (D-46) ใช้จริงไม่ได้ แก้ Live Test Mode analyzer error แล้ว** — pete รัน Live Test Mode เจอ `dart analyze` error จริง 2 จุด (`home_widget.dart`, `String?` ผูกกับ parameter `String`) ต้นเหตุ: custom function return value เป็น `String?` เสมอ (เหมือน param, D-47) ผูกเป็น `PostgresFilter.value` ของ `iLike` ตรง ๆ ไม่ผ่าน ลองย้ายไปพักที่ page-state field ก่อนคิดว่า non-nullable — **ผิด ทุก state field เป็น nullable หมดไม่ว่า type อะไร** (ยืนยันจาก `generated_code/lib/home/home_model.dart`) สรุปจริง: **`equalTo` เท่านั้นที่ null-safe (compile ผ่าน `.eqOrNull()` เสมอ) `iLike`/`like`/`contains` ไม่มี variant นี้เลย ผูกกับค่าไดนามิกไม่ได้** ถอย search กลับเป็น exact match ลบ `wrapSearchPattern` ทิ้ง (`app.removeCustomFunction`, เรียนบทเรียนจาก D-47 ทันที) ใช้ `If(Equals(searchQuery, ''), then: [query ไม่มี title filter], orElse: [มี filter])` (typed conditional action ตัวจริงคือ `If` ไม่ใช่ `Actions.conditional`) แทนที่ trick "iLike('') แมตช์ทุกอย่าง" ที่ใช้ไม่ได้แล้ว ยืนยันจาก `generated_code/`: ไม่มี `.ilike(` เหลือเลย, `custom_functions.dart` เหลือ 5 ฟังก์ชันสะอาด รายละเอียด `DECISIONS.md` D-48, pattern ใหม่ `PATTERNS.md` PT-27 ข้อ 6-7 — **ยังไม่ได้ให้ pete รัน Live Test Mode ซ้ำยืนยัน** · ค้นหาแบบ substring จริงต้องทำ RPC ผ่าน custom action (P-05) — ยังไม่ทำ รอ pete ตัดสินใจ
> 🔴 **D-47: `shuffleProducts` orphan ทำ `custom_functions.dart` ทั้งไฟล์ compile ไม่ผ่าน — แก้แล้ว** — pete รายงานเจอ error ตอนเปิด FlutterFlow editor (2 จุด: `getOtherUsers` เก่าที่ไม่ได้แตะ กับ `shuffleProducts`) ต้นเหตุ: D-45 ลบแค่บรรทัด declare `shuffleProducts` ออกจากสคริปต์ ไม่ได้เรียก `app.removeCustomFunction(...)` เลยยังค้างบนโปรเจกต์จริงพร้อม signature ผิดเพี้ยน (`ProductsReviewViewRow?` ไม่ใช่ list ทั้ง arg/return ทั้งที่สคริปต์ตอนประกาศระบุ `listOf(...)`) body ยังทำเหมือนเป็น list (`List<...>.from(items)`) — type mismatch จริง `flutterflow ai run` ไม่เคยจับได้เพราะไม่ dart-compile custom code เพราะ custom function ทุกตัวรวมคอมไพล์ไฟล์เดียวกัน ตัวเดียวพังทำทั้งไฟล์พัง กระทบฟังก์ชันอื่นที่ไม่เกี่ยวข้องด้วย แก้ด้วย `app.removeCustomFunction('shuffleProducts')` ยืนยันจาก `generated_code/`: เหลือ 6 ฟังก์ชันสะอาดครบ ไม่กระทบ `Home` เลย รายละเอียด `DECISIONS.md` D-47, pattern ใหม่ `PATTERNS.md` PT-27 ข้อ 5
> ✅ **D-46: `Home` ค้นหาแบบ substring จริงแล้ว + trigram index** — D-45's `iLike` ไม่มี `%...%` wrap ทำให้ค้นหาเป็น exact match เท่านั้น (และทำให้ pull-to-refresh ตอนไม่ได้ค้นหาโชว์ grid ว่างเปล่า — บั๊กจริงที่เจอเพิ่มระหว่างแก้) ปิดด้วย custom function `wrapSearchPattern(keyword) => '%$keyword%'` ผูกเป็น `PostgresFilter.value` ตรง ๆ **ใช้ได้จริง** (คนละ code path จาก D-45's `SetState` ที่พัง — ยืนยันจาก compiler source + push จริง) เพิ่ม GIN trigram index (`pg_trgm`) บน `products.title` รองรับ substring search ให้เร็วขึ้นเมื่อข้อมูลโต 🔴 เจอกับดักใหม่: custom function param เป็น `nullable` เสมอในโค้ดที่ generate จริงไม่ว่า DSL type จะระบุยังไง (`String? keyword` แม้ประกาศ `string`) พังตอน build แอปถ้าไม่ null-check พุชแรกพลาดจุดนี้ (push ผ่านเฉย ๆ เพราะ validate แค่ proto ไม่ dart-compile custom code) จับได้จากอ่าน `generated_code/` แก้ด้วย `custom_code_helpers.updateCustomFunction` 🔴 **ลอง empty-state UI (pete ขอเพิ่ม) แล้วไม่สำเร็จ ถอนออกทั้งหมด** — ทั้ง raw-proto `listLength()` condition และ custom-function-derived boolean state (`hasNoResults`) ถูก backend ปฏิเสธทั้งคู่ (error เดียวกับ D-45 แม้ target เป็น `bool` ไม่ใช่ list — แก้ทฤษฎีเดิมว่าปัญหาไม่ได้อยู่ที่ type ปลายทาง แต่อยู่ที่ custom function รับ `List<PostgresRow>` เป็น argument) รายละเอียด `DECISIONS.md` D-46, pattern ใหม่ `PATTERNS.md` PT-27 (ต่อท้าย) — **ยังไม่ได้ให้ pete ทดสอบผ่านแอปจริง**
> ก่อนหน้า: **D-45: `Home` ค้นหา + สุ่มลำดับสินค้า + pull-to-refresh** — สุ่มฝั่ง Dart (custom function) ทำไม่ได้จริง (`SetState` ของ `List<PostgresRow>` field รับค่าจาก custom function ไม่ได้ — backend validate fail ทุกครั้งแม้ type ตรง) ย้ายไปสุ่มที่ SQL แทน: เพิ่ม `products_review_view.shuffle_key` (`random()`) แล้ว `ORDER BY shuffle_key` แทน `created_at` ทุก query (onLoad/search/26 category chip) 🔴 เจอกับดักใหม่ 2 ข้อ: (1) เปลี่ยน `orderBys` โดยใช้ `outputAs` เดิม push ผ่านแต่โค้ดที่ export ไม่เปลี่ยนเลย ต้องเปลี่ยน `outputAs` ด้วยเสมอ (2) ลืม retire `ensureReplaced` ของช่องค้นหาตาม PT-16/D-27 ทำให้พุชถัดมา error เฉพาะ widget นั้น แก้ด้วยย้ายไปผูก `onChanged`/`onSubmitted` ผ่าน `page.ensureActions` แทน รายละเอียด `DECISIONS.md` D-45, pattern ใหม่ `PATTERNS.md` PT-27
> ก่อนหน้า: **D-44: `ProductDetails` โชว์รูปที่ 2/3 สำเร็จแล้ว** — เลี่ยง D-43's `ListView`/`item[]` ทั้งหมด ใช้ scaffold-level `databaseRequest` + `nodeKeyRef` แทน (เทคนิคเดียวกับ `ProfileUser`/`HomeAdmin`) — query แยกอิสระคนละตัวจาก list เดิม ไม่แตะ hero image/text fields เลย แสดงรูป 2/3 เป็น `Row(scrollable: true)` เคียงข้างกัน (ไม่ใช้ `Carousel`/`PageView` ตามที่ pete สั่ง) 🔴 เจอกับดักใหม่ระหว่างทำ: static `visible: false` ไม่ซ่อน widget จริงในโค้ดที่ export (ต้องใช้ conditional/`variable:`-based visibility เท่านั้น) แก้แล้วในพุชที่ 2 รายละเอียด `DECISIONS.md` D-44, pattern ใหม่ `PATTERNS.md` PT-26 — **ทดสอบผ่านแอปจริงโดย pete แล้ว — ผ่าน**
> ก่อนหน้า: **D-43: multi-photo carousel + tap-to-view บน `ProductDetails` ทำไม่สำเร็จด้วยวิธี `ListView`/`item[]`** — พบขีดจำกัดจริงของ FlutterFlow AI SDK 3 ข้อจาก itemBuilder เดียวกัน (isolate แยกทีละตัวแปรกว่า 20 พุช): (1) `item['field']` ใช้เป็น action param ไม่ได้เลยไม่ว่า action ไหน (2) `item[]` ผ่าน Dart helper function ไม่ได้ (3) 🔴 **มี `Image` widget ผูก `item[]` ได้แค่ 1 ตัวต่อ itemBuilder** — บล็อกทั้ง carousel และ tap-to-view เตรียม schema ไว้แล้ว (`second_image_url`/`third_image_url`/`has_second/third_image`) แต่ยังใช้ไม่ได้ รายละเอียด `DECISIONS.md` D-43, pattern ใหม่ `PATTERNS.md` PT-25
> ✅ **D-42: `ProductDetails` โชว์รูปสินค้าจริงแล้ว** — เดิมช่องรูปเป็น `Icon(Icons.image)` คงที่ ไม่เคยผูกกับข้อมูลเลยสักจุด (ซากจาก template เดิม) เพิ่ม `products_review_view.has_image` (boolean) แล้ว rebuild `ProductDetailsContent` itemBuilder ใหม่ทั้งก้อนผ่าน `ensureReplaced` (จำเป็นเพราะ `ItemRef()`/`item[]` ใช้นอก itemBuilder สดไม่ได้) — โชว์รูปจริงเมื่อมี, fallback icon เมื่อไม่มี (ไม่ crash) field อื่นทั้งหมดคัดลอกจาก `generated_code` เป๊ะ ไม่กระทบ รายละเอียด `DECISIONS.md` D-42 — **ยังไม่ได้ให้ pete ทดสอบผ่านแอปจริง**
> ก่อนหน้า 2026-08-18: **D-41: `chatMessages` bubble UI สองฝั่ง + ส่งรูปได้จริง** — เปลี่ยนจาก list คอลัมน์เดียวเป็น bubble ซ้าย/ขวา เพิ่มปุ่มแนบรูปเข้า bucket `chat-images` (มีอยู่แล้ว) + ดูรูปเต็มผ่าน component `FullImageViewer` (แตะ ไม่ใช่กดค้าง — DSL ไม่มี `onLongPress`) แก้บั๊ก null-crash (`has_message`/`has_image` ใน view แทนเทียบ `== ''`) + ComposeBar ไม่ติดขอบล่าง (`Expanded` แทน `shrinkWrap`) + `outputAs` ชนกันข้าม widget รายละเอียด `DECISIONS.md` D-41, pattern ใหม่ `PATTERNS.md` PT-24 — **ทดสอบผ่านแอปจริงโดย pete แล้วทั้งหมด**
> ✅ **D-40: แก้ `chatList` ว่างเปล่า — เป็น layout crash ไม่ใช่บั๊ก RLS/data** — pete รายงานพร้อม console log (`Assertion failed: box.dart:2251` ซ้ำๆ) ตรวจ Supabase (db-verifier ยืนยัน `chat_summary` คืนแถวถูกต้อง) + widget binding ก่อนแล้วพบว่าถูกทั้งคู่ ตัวปัญหาจริงคือ `ChatListItems` (ListView) เป็นลูก `Column` โดยไม่ได้ห่อ `Expanded` และไม่ได้ตั้ง `shrinkWrap` → unbounded height ทำให้เรนเดอร์อะไรไม่ได้เลยไม่ว่าจะมีข้อมูลกี่แถว แก้ด้วย `shrinkWrap: true` (raw proto, ไม่มี typed/fast-lane op) ยืนยันจาก `generated_code/` แล้ว รายละเอียด `DECISIONS.md` D-40 — **ยังไม่ได้ให้ pete ทดสอบซ้ำ**
> ✅ **D-39: Filter chip highlight ตามการเลือกจริงแล้ว** — บั๊กหลัง D-37: filter ทำงานถูกแต่สี chip ไม่เปลี่ยนตามที่กด (`Button.color` เป็น static, ไม่ผูก state) แก้ด้วยคู่ Button (selected/unselected) สลับด้วย `visible: Equals(...)`/`Not(...)` — typed DSL ล้วน ไม่มี raw proto ยืนยันจาก `generated_code/`: `if (_model.selectedX == N) Button(...)` ครบทุก chip ทั้ง `Home`/`Mypost` รายละเอียด `DECISIONS.md` D-39 — **ยังไม่ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-38: `Home` AllList เป็น GridView 2 คอลัมน์แบบ e-commerce แล้ว** — เปลี่ยนจาก `ListView` แถวเดียว ไม่แตะกลไก filter เดิมเลย (onLoad + 13 category chip ยังยิง query เหมือนเดิม) เพิ่มคอลัมน์ `first_image_url` (`image_urls[1]`) เข้า `products_review_view` เพราะ DSL ไม่มี list-index operator ยืนยันจาก `generated_code/`: `GridView.builder` + รูปจริงผูกแล้ว รายละเอียด `DECISIONS.md` D-38 — **ยังไม่ทดสอบผ่านแอปจริงโดย pete** โดยเฉพาะเคสสินค้าไม่มีรูป (ยังไม่มี placeholder)
> ✅ **D-37: Category/Status filter chips ใช้งานได้จริงทั้งคู่แล้ว** — บั๊กจริงคือ `Chip` widget (D-36) ห่อ `InkWell.onTap` รอบ Material `ChoiceChip` ที่มี gesture handler ของตัวเองอยู่แล้ว สองอันแย่ง tap กัน แก้ด้วยการเปลี่ยนเป็น `Button` ทั้ง `Home`/`Mypost` (ยืนยันจาก `generated_code/`: `FlutterFlowChoiceChips` เหลือ 0 จุด) `Mypost` ยังย้ายจาก widget-level query ไปเป็น onLoad+state pattern แบบ `Home` เพื่อให้ status chip กรอง list ได้จริง (เดิมติด platform limitation ตาม D-36) รายละเอียดเต็ม `DECISIONS.md` D-37 — **ยังไม่ทดสอบผ่านแอปจริงโดย pete**
> ✅ **D-35: แก้ filter `seller_id` ของ `Mypost`** — เดิม widget-level query ไม่มี filter เลย (D-32 ข้อ 5) เพิ่ม `seller_id = currentUserUid` ผ่าน `page.mutateNode` บน `ListView_7h86cihf` ยืนยันจาก `generated_code/` แล้วว่า query เปลี่ยนจริง รายละเอียด `DECISIONS.md` D-35 — **ยังไม่ทดสอบผ่านแอปจริง**
> ✅ **D-34: L1 confirm-email (D-20) ปลดล็อกจริง** — เลิกส่ง OTP ตรงถึง student เปลี่ยนเป็น Send Email Hook → Edge Function `send-otp-email` → Resend → relay เข้า admin inbox เดียว ยืนยัน end-to-end สำเร็จผ่านแอปจริงแล้ว (`mju6606105382@mju.ac.th`) รายละเอียด+กับดัก `DECISIONS.md` D-34 — **หนี้ที่เหลือ:** manual relay ไม่ scale, rate limit email 30/ชม. เป็นค่าดีบักชั่วคราว ทั้งคู่ต้องแก้ก่อน production
> ✅ **D-33: ปิดช่องโหว่ `admin_sales_by_seller`** — เพิ่ม `AND private.is_admin()` ในตัว view เอง ไม่พึ่ง RLS ของ `products` (allow-all, D-03) อีกต่อไป ยืนยันด้วย impersonation test จริง (user ธรรมดา 0 แถว, admin เห็นถูกต้อง) รายละเอียด `DECISIONS.md` D-33 — ปิดข้อ 2 ของคิว D-32 แล้ว เหลือ 4 ข้อ
> 🔴 **D-32: DoD audit ทั้งโปรเจกต์ (db-verifier + ui-checker คู่ขนาน) พบบั๊กจริง 4 จุด** — (1) L1: 2 บัญชี `auth.users` ไม่มีแถวใน `"Profile"` เลย ~~(`mju6606105382`/`mju6606105386`)~~ **`mju6606105382` แก้แล้ว (ลบบัญชีเก่า สมัครใหม่สะอาด, D-34) `mju6606105386` ลบทิ้งแล้ว (pete ยืนยัน 2026-08-24) — ปิดทั้งข้อ** (2) ~~L8: `admin_sales_by_seller` ไม่มี admin gate~~ **ปิดแล้ว (D-33)** (3) L4: ยืนยันแล้วว่า Realtime ไม่มีเลย ไม่ใช่แค่ "ยังไม่ยืนยัน" (4) จุดแดง unread (D-31) มีบั๊ก stale-state บน `chatList`/`Notifications`/`ReportsFeedback` — ไม่หายจนกว่าจะออกจากหน้าจริง (5) `MyPost` มีอยู่แล้วจริง (ไม่ใช่ "ยังไม่สร้าง") query ไม่ filter `seller_id` เลย โชว์ของทุกคน ~~— แก้แล้ว (D-35)~~ รายละเอียด `DECISIONS.md` D-32
> ก่อนหน้า 2026-08-17: **D-31: จุดแดง glow บอกยังไม่อ่าน (หายเมื่อแตะ) บน `chatList`/`Notifications`/`ReportsFeedback`** — `chat_user.last_read_at` (ต่อสมาชิก) + `chat_summary.is_unread`, `reports.is_read` + RPC `mark_chat_read`/`mark_report_read` (บล็อก non-admin จริง) 🔴 พบว่า pete rename หน้า "Reports" เป็น "ReportsFeedback" ตรงใน FlutterFlow editor ทำให้ `ensurePage('Reports', ...)` เดิมเกือบสร้างหน้าซ้อน (จับได้ตอน push fail แก้แล้ว) รายละเอียด `DECISIONS.md` D-31
> ก่อนหน้า 2026-08-16: **D-29/D-30: L4 (chat) เริ่มและปิด Supabase ฝั่งสมบูรณ์ + FlutterFlow ฝั่งข้อความล้วนใช้งานได้จริงครบ 3 ทางเข้า** — RLS membership-based (เดิม allow-all), `find_or_create_chat`/`find_or_create_chat_with_admin`/`is_chat_member`/`get_my_chats` + trigger auto-update `last_message`, รองรับส่งรูปที่ schema (ยังไม่ทำฝั่ง UI), `chatList` ผูกข้อมูลจริง + หน้า `chatMessages` ใหม่ + ปุ่ม "แชทกับผู้ขาย" บน `ProductDetails` + ปุ่ม "ติดต่อแอดมิน" ต่อจริงแล้ว (D-30) + ทางเข้า `chatList` ใน `HomeAdmin` drawer (D-30) รายละเอียด `layers/L4-chat.md` + `PATTERNS.md` PT-09/PT-22/PT-23
> ก่อนหน้า 2026-08-15: D-24–D-27 (reject-flow, reports, addproduct flash bug, ContactAdminButton) ทดสอบผ่านแอปจริงโดย pete แล้ว
> ก่อนหน้า 2026-08-14: auth backend = Supabase (D-21) · L8 `HomeAdmin` approve/reject + `notifications` table + `Notifications` page + bell icon (D-22/D-23)
---

## 🔥 คิวถัดไป

0r. **[D-65] ทดสอบรีพอร์ตผู้ใช้ผ่านแอปจริง** — เช็ค: เปิดโปรไฟล์คนอื่น (ผ่าน `ProductDetails`/`BannedUsers`/`UserDirectory`) → เห็นปุ่ม "รายงานผู้ใช้", เปิดโปรไฟล์ตัวเอง → **ไม่เห็น**ปุ่มเลย, กดปุ่ม → กรอกเหตุผล → ส่งสำเร็จ, ลองรายงานคนเดิมซ้ำตอนรายงานเดิมยัง pending → ควรถูกกัน (unique index — ตรวจว่า error หรือเงียบ ไม่มี onFailure hook), admin เปิด `ReportsFeedback` → เห็นรายงานประเภทผู้ใช้ในลิสต์ → เปิด `ReportDetail` → เห็นบล็อก "ผู้ใช้ที่ถูกรายงาน" ถูกต้อง (ไม่เห็นบล็อก "สินค้า" ปนมา), กดปุ่ม "ระงับผู้ใช้รายนี้" → `BanUserSheet` เปิดถูกคน ระงับสำเร็จจริง, เปิด `ReportDetail` ของรายงานสินค้าเดิม (ก่อน D-65) → ต้องยังทำงานปกติไม่พัง (regression check)
0s. **[D-64] ทดสอบ `reviews` (ให้คะแนนผู้ขาย) ผ่านแอปจริง** — ต้องมีธุรกรรมที่ปิดการขายแล้วจริง (ผ่าน `mark_product_sold`, D-59) เช็ค: ฝั่งผู้ซื้อเปิด `ProductDetail` ของสินค้าที่ตัวเองซื้อ (status=sold) → เห็นปุ่ม "ให้คะแนนผู้ขาย", กดปุ่ม → เห็น sheet มี Slider 1-5 + ช่องคอมเมนต์, กดส่ง → snackbar สำเร็จ + insert เข้า `reviews` จริง, เปิดหน้าเดิมซ้ำ → ปุ่มควรหาย/ไม่ให้รีวิวซ้ำ (ปุ่มอาจยังไม่หายทันทีเพราะ page-level databaseRequest ไม่มี refetch ง่าย ๆ — ตรวจว่า reload หน้าใหม่แล้วปุ่มหายจริง), ฝั่งผู้ขาย/user อื่นที่ไม่ใช่ผู้ซื้อ → ไม่เห็นปุ่มเลย, สินค้าที่ยังไม่ขาย (status≠sold) → ไม่เห็นปุ่มเลย, พยายามให้คะแนนธุรกรรมเดียวกันซ้ำผ่าน insert ตรง (ถ้าทดสอบได้) → ต้องถูก RLS/unique ปฏิเสธ
0t. **[D-61/D-62/D-63] ทดสอบ `Home` search/filter ผ่านแอปจริง** — เช็ค: ค้นหาด้วยคำบางส่วน (ไม่ใช่ชื่อเป๊ะ) เจอสินค้าจริง, ค้นหาคำที่อยู่ใน description เจอด้วย, กรองหมวดหมู่แล้วค้นหาซ้อน (คนละแกน — กดอันหนึ่งต้องล้างอีกอัน เหมือนเดิม), ใส่ราคาต่ำสุด/สูงสุดกดปุ่ม "กรอง" → เห็นเฉพาะสินค้าในช่วงราคา, สลับหมวดหมู่/ค้นหาใหม่หลังตั้งราคาแล้ว → **ตัวกรองราคาต้องยังอยู่** (ไม่ถูกล้าง), เคลียร์ราคา (ลบข้อความในช่องแล้วกด "กรอง" ใหม่) → กลับมาเห็นครบ, ใส่ตัวอักษร/ค่าว่างในช่องราคาแล้วกด "กรอง" → ไม่ crash, pull-to-refresh ระหว่างค้นหา/กรองหมวดหมู่ → ยังเคารพตัวกรองปัจจุบัน, สินค้า pending/rejected/sold ต้องไม่หลุดมาในผลค้นหาทุกกรณี
0u. **[D-60] ทดสอบ Realtime (chat + Notifications) ผ่านแอปจริง** — ใช้บัญชีเพื่อน pete จริง 2 บัญชี (ลบทิ้งหลังทดสอบ) เช็ค: เปิด `chatMessages` ห้องเดียวกันพร้อมกัน 2 เครื่อง/2 บัญชี → ฝั่ง A ส่งข้อความ → ฝั่ง B เห็นขึ้นเองโดยไม่ต้อง refresh/ออกเข้าใหม่ (และกลับกัน), เปิด `chatList` ค้างไว้ระหว่างที่อีกฝั่งส่งข้อความมาในห้องใดห้องหนึ่ง → preview/จุดแดง unread อัปเดตสดโดยไม่ต้องออกจากหน้า, เปิด `Notifications` ค้างไว้แล้วให้แอดมิน reject ประกาศ (หรือแบน/ปลดแบน) ของบัญชีที่เปิดหน้าอยู่ → รายการใหม่ขึ้นเองสด, **สำคัญ:** เข้าห้องแชทผ่านปุ่ม "แชทกับผู้ขาย" (ไม่ใช่ผ่าน `chatList`) → เช็คว่าชื่อผู้ส่งไม่ว่างเปล่า (memberNames/userIds มาจริง ไม่ใช่แค่ตอนเข้าทาง `chatList`), **สำคัญ:** ล็อกอินเป็นบัญชีที่ 3 ที่ไม่ได้อยู่ในห้องแชทของ A/B เลย เปิดค้างไว้ → ต้อง**ไม่**เห็น/ไม่ได้รับ realtime event ของห้องที่ตัวเองไม่ได้อยู่ (RLS ต้องกันได้จริง ไม่ใช่แค่ query ปกติ)
0v. **[D-59] ทดสอบ Layer 5 (ปิดการขาย) ผ่านแอปจริง** — ต้องมี 2 บัญชีทดสอบ (ผู้ขายมีประกาศ `available` อย่างน้อย 1 ชิ้น + ผู้ซื้อ) เช็ค: เปิดแชทระหว่าง 2 บัญชี → ฝั่งผู้ขายเห็นส่วน "ทำเครื่องหมายว่าขายแล้ว" พร้อม list ประกาศตัวเอง → แตะเลือกประกาศ → เห็นข้อความยืนยัน → กดยืนยัน → snackbar สำเร็จ + list หายจากตัวเลือก, กลับไป `Home` เช็คว่าของหายจาก grid จริง (ทั้ง onLoad/ค้นหา/ทุก category chip), ไปที่ `Mypost` เช็คว่าเห็น badge "ขายแล้ว" บนแถวนั้น (ของยังอยู่ในลิสต์ ไม่หาย), แตะเข้า `ProductDetails` ของประกาศนั้นในฐานะผู้ขาย → เห็นชื่อผู้ซื้อ, ล็อกอินเป็นแอดมินเปิดหน้าเดียวกัน → เห็นผู้ซื้อเหมือนกัน, ล็อกอินเป็นบัญชีอื่น (ไม่ใช่ผู้ขาย/ผู้ซื้อ/แอดมิน) เปิดหน้าเดียวกัน → **ต้องไม่เห็น**ชื่อผู้ซื้อ, กลับไปแชทเดิม → ส่วน "ทำเครื่องหมายว่าขายแล้ว" ต้องหายไป/เปลี่ยนเป็นข้อความ "ขายแล้วในแชทนี้" (ไม่ให้ขายซ้ำในแชทเดิม), เช็ค `HomeAdmin` → การ์ด "ยอดขายตามผู้ขาย" มีตัวเลขจริงของผู้ขายคนนี้ขึ้นแล้ว (เดิมเป็น 0 เพราะยังไม่เคยมีการขาย), ทดลองบัญชีที่ไม่มีประกาศ `available` เลย → ไม่เห็นส่วน "ทำเครื่องหมายว่าขายแล้ว" ในแชทของตัวเองเลย
0w. **[D-58] ทดสอบ admin advertisement posts + likes ผ่านแอปจริง** — เช็ค: `HomeAdmin` side-nav → "ประกาศ/แบนเนอร์" → อัปโหลดรูป (เห็น preview จริง ไม่ใช่ค้างที่ไอคอน placeholder) → กรอกหัวข้อ/รายละเอียด (optional ทั้งคู่ ลองข้ามได้) → กดโพสต์ → กลับ `HomeAdmin` แล้วไปเช็ค `Home` ว่าโพสต์ขึ้นจริงบน `ListView_6etuspo6` (บนสุด, ใหม่ก่อน), กดหัวใจไลก์ → นับขึ้น 1 → กดอีกทีเลิกไลก์ → นับกลับ 0, ปิดแอปเปิดใหม่เช็คว่าค่าไลก์ค้างจริง (ไม่ reset), user ธรรมดา (ไม่ใช่ admin) พยายามเข้า route `admin-create-post` ตรง ๆ ต้องเด้งกลับ `Home` พร้อม snackbar
0x. **[D-57] ทดสอบ Dropdown ชั้นปี/คณะบน `ProfileUser` ผ่านแอปจริง** — เช็ค: เปิด Edit Profile → เลือกแค่ชั้นปี (ไม่แตะชื่อ/คณะ) → กดบันทึก → เช็คว่า `full_name`/`faculty_id` เดิมไม่ถูกทับเป็น NULL (แค่ `year_of_study` เปลี่ยน), ทำสลับกันทีละฟิลด์ให้ครบทั้ง 3 แบบ, เช็คว่าข้อมูลที่บันทึกไปโผล่ใน `UserProfileCard` ของตัวเองจริง (เช่นให้อีก user เปิดดูโปรไฟล์เรา หรือดูจาก `UserDirectory`)
0y. **[D-56] ทดสอบหน้า `UserDirectory` ผ่านแอปจริง + ให้ pete แก้ชื่อคณะจริง** — เช็ค: กดไอคอนใหม่จาก `Home` header → เห็นรายชื่อผู้ใช้ทั้งหมด (ไม่มีคนถูกแบนปน), กรองแต่ละชั้นปี (1-4)/แต่ละคณะ (A-D) ทีละแกน, ค้นหาชื่อ exact match, แตะแถวเปิด `UserProfileCard` ถูกคน, แบน user ทดสอบ 1 คนแล้วเช็คว่าหายจาก `UserDirectory` แต่ admin ยังเปิด `UserProfileCard` จากหน้า `BannedUsers` ได้ปกติ — **pete ต้องแก้ชื่อ 4 คณะจาก placeholder "คณะ A/B/C/D"** ผ่าน `UPDATE faculties SET name='...' WHERE id=...` แล้วแก้ **ทั้ง 2 จุด** ใน `dsl/edit.dart` ให้ตรงกัน: `facultyChipLabels` (`UserDirectory` filter, D-56) **และ** `profileFacultyLabels` (`ProfileUser` Dropdown, D-57 — คนละ const แยกกัน ไม่ได้ใช้ร่วม) + push ใหม่ (chip/dropdown label ไม่ query ตาราง `faculties` ตรง ๆ แก้ DB อย่างเดียวไม่พอ)
0z. **[D-55] ทดสอบป๊อปอัพโปรไฟล์ผู้ใช้ผ่านแอปจริง** — เช็ค: user ธรรมดาแตะชื่อผู้ขายใน `ProductDetails` เห็นการ์ดโปรไฟล์ผู้ขาย (ทั้งดูของคนอื่นและของตัวเอง), admin แตะชื่อในแถว `BannedUsers` เห็นการ์ดโปรไฟล์คนที่ถูกแบนโดยไม่ชนปุ่ม Unban ข้าง ๆ, สลับดูโปรไฟล์ 2 คนติดกันไม่เห็นข้อมูลคนแรกค้าง, กดปิด/แตะ barrier ปิดการ์ดได้ทั้งคู่, โปรไฟล์ที่ไม่มี avatar/bio/ชั้นปี/คณะ (เกือบทุกคนตอนนี้) ต้องเห็น placeholder/"ยังไม่ระบุ..." ไม่ใช่ค่าว่างเปล่า
0a. **[D-53] ทดสอบหน้า `BannedUsers` ผ่านแอปจริง** — เช็ค: `HomeAdmin` → กดการ์ด "ผู้ใช้ถูกระงับ" → เห็นเฉพาะคนที่ถูกแบน, ตัวเลขบนการ์ดตรงกับจำนวนแถวในหน้านี้, กด Unban แล้วกดปุ่มรีเฟรช (ไม่ auto-refetch ตั้งใจ) แถวต้องหายไปจริง, `banned_at`/`ban_reason`/`banned_by_name` โชว์ค่าถูกต้อง
0. **[D-52] ทดสอบระบบ ban ผ่านแอปจริง** — โค้ดครบทั้ง DB+UI แล้ว ยังไม่เคยกดจริง เช็ค: `HomeAdmin` → "ผู้ใช้งาน" → `ManageUsers` เห็นรายชื่อครบ, แถวตัวเอง/แอดมินคนอื่นไม่มีปุ่มระงับ, กดระงับไม่กรอกเหตุผลต้องขึ้น error ไม่ใช่เงียบ, กดปุ่มรีเฟรชหลังปลดระงับ (unban ไม่ auto-refetch ตาม PT-29 §1 — จงใจ), ปุ่ม "ระงับผู้ขายรายนี้" ใน `ReportDetail` ใช้ sheet เดียวกัน, บัญชีที่ถูกแบน login แล้วเห็น popup พร้อมกด "ติดต่อแอดมิน" ส่งข้อความได้จริง, ลงประกาศ/แชทผู้ขายไม่ได้แต่ `Mypost` ยังเห็นของตัวเองครบ, user ปกติอีกคนไม่เห็นประกาศของคนที่ถูกแบน — 🟡 รู้อยู่แล้ว ไม่ต้องรายงานซ้ำ: ปุ่มปลดระงับไม่ auto-refresh, popup ไม่จำว่าเคยปิด (ขึ้นทุกครั้งที่เข้า Home ตั้งใจ), ยังไม่ซ่อนปุ่มลงประกาศ/ComposeBar ของผู้ถูกแบน (RLS บล็อกอยู่แล้ว แต่ UI ยังกดได้แล้วเงียบ)

~~1. **[D-32] `mju6606105386@mju.ac.th` ยังค้าง**~~ **ลบทิ้งแล้ว (pete ยืนยัน 2026-08-24)**
1b. **[D-49] ทดสอบจุดแดง unread ผ่านแอปจริง** — `chatList`/`Notifications`/`ReportsFeedback` แก้ stale-state แล้ว (refetch หลังแตะ) ยืนยันแค่จาก `generated_code/` ยังไม่เคยกดจริงในแอป
2. ~~**ทดสอบ L4 (chat) ผ่านแอปจริง**~~ **ทดสอบแล้ว (D-40/D-41, 2026-08-18)** — `chatList` ขึ้นถูก, ส่งข้อความ/รูปได้จริง, bubble UI ถูกฝั่ง, ComposeBar ติดขอบล่าง, ดูรูปเต็มได้ — จุดแดง unread stale-state แก้แล้ว (D-49, ดูข้อ 1b)
3. ~~**L4: ทำส่งรูปภาพ**~~ **แก้แล้ว (D-41, 2026-08-18)** — เหลือ **Realtime** (ยืนยันว่าไม่มีเลย D-32, ยังไม่ทำ)
4. ~~**[D-32] แก้ filter ของ `MyPost`**~~ **แก้แล้ว (D-35, 2026-08-18)** — เหลือทดสอบผ่านแอปจริงด้วย user ธรรมดาที่มีประกาศหลายสถานะ (pending/approved/rejected) ว่าเห็นเฉพาะของตัวเองจริง
5. ~~**[D-36] ทำให้ status chip บน `Mypost` กรอง list จริง**~~ **แก้แล้ว (D-37, 2026-08-18)** — ย้าย `Mypost` ไป onLoad+state pattern แบบ `Home` เหลือทดสอบผ่านแอปจริงว่ากด chip แล้ว list กรองถูกต้องจริง (category ของ `Home` ด้วย)
6. **[D-38/D-39] ทดสอบ `Home` grid layout + filter chip highlight ผ่านแอปจริง** — ยังไม่เคยเปิดแอปดูทั้งคู่ เช็ค: การ์ดจัดเรียง 2 คอลัมน์ไม่ถูกตัด, กดการ์ดยังเข้า `ProductDetails` ถูก, กด chip แล้วสี highlight ตามจริงทั้ง `Home` (หมวดหมู่)/`Mypost` (สถานะ) — 🔴 **สินค้าไม่มีรูปบน `Home` ยัง crash ได้จริง** (`firstImageUrl!` force-unwrap ไม่มี fallback) `products_review_view.has_image` มีแล้ว (D-42, ทำไว้ให้ `ProductDetails`) แค่ยังไม่เอาไปใช้กับ `Home` grid — ทำตอนเจอปัญหานี้จริงหรือก่อน production
7. ~~**[D-42/D-44] `ProductDetails` โชว์รูปสินค้าจริงแล้ว (รูปแรก + รูปที่ 2/3)**~~ **ทดสอบผ่านแอปจริงโดย pete แล้ว — ผ่าน (2026-08-19)**
8. **[D-45/D-48/D-50] ทดสอบ `Home` ค้นหา + ปุ่มค้นหา + สุ่มลำดับ + pull-to-refresh ผ่าน Live Test Mode/แอปจริง** — ยังไม่เคยยืนยันหลัง D-50 เช็ค: **Live Test Mode ไม่มี Analyzer Errors แล้ว**, กดปุ่ม "ค้นหา" ทำงานเหมือนกด IME submit จริง, ลำดับสินค้าเปลี่ยนทุกครั้งที่เข้าหน้า/pull-to-refresh, ค้นหาด้วยชื่อเป๊ะ (case-insensitive) เจอจริง, pull-to-refresh ตอนไม่ได้ค้นหาโชว์ของครบไม่ว่างเปล่า, กด category chip ล้างช่องค้นหาจริง, สินค้า pending/rejected ไม่หลุดมาในผลค้นหา — 🟡 รู้อยู่แล้วว่าค้นหาเป็น exact match เท่านั้น (D-48, substring ทำไม่ได้จริง) และยังไม่มี empty-state UI (D-46) ไม่ต้องรายงานซ้ำถ้าเจอ (ดู `layers/L3-browse-search.md` ค้างอยู่)
9. ~~**[D-47] `shuffleProducts` orphan ทำ custom_functions.dart พังทั้งไฟล์**~~ **แก้แล้ว (2026-08-19)** — ยืนยันจาก `generated_code/` ว่าลบออกจริงและฟังก์ชันอื่นสะอาด เหลือให้ pete เปิด FlutterFlow editor ยืนยันว่า error ที่เจอ (ทั้ง `getOtherUsers` และ `shuffleProducts`) หายไปจริงแล้ว
10. **[D-51] ทดสอบซ่อนปุ่ม "รายงาน"/"แชทกับผู้ขาย" ผ่านแอปจริง** — เข้า `ProductDetails` ของประกาศตัวเอง (ผ่าน `MyPost`) ต้องไม่เห็นทั้ง 2 ปุ่ม, เข้าประกาศคนอื่นต้องเห็นปกติ ยืนยันแค่จาก `generated_code/` ยังไม่เคยกดจริงในแอป

🟡 **ไม่ใช่คิวด่วนแต่ยังไม่ปิด — L1 confirm-email production-readiness** ดู `DECISIONS.md` D-34 หัวข้อหนี้

---

## สถานะ 8 Layers

| L | ชื่อ | Supabase | FlutterFlow | หมายเหตุ |
|---|---|---|---|---|
| 1 | Auth & User Profiles | ✅ ปิดแล้ว 🔴 พบ 2 บัญชีไม่มี `Profile` (D-32) | 🟨 Login/SignUp/Home/HomeAdmin/`ProfileUser` ทำงานจริง เหลือ Confirm Email (D-20) | auth backend = Supabase แล้ว (D-21) · OTP ติด email deliverability |
| 2 | Product Listings + Storage | 🟨 อัปจริงผ่านแอปแล้ว เหลือเทส reject >5MB/ผิดชนิด · **`category_id` เป็น NOT NULL แล้ว (D-61)** | 🟨 `addproduct` insert ผ่านแอปจริง + แก้บั๊กแฟลชไป Login (D-26) · `MyPost` filter `seller_id` แก้แล้ว (D-35) + status chip กรอง list จริงแล้ว (D-37) ยังไม่ทดสอบผ่านแอปจริง | seed `CAT` 12 หมวด (ยังคง manual, ไม่ทำ admin UI — pete ตอบแล้ว) · bucket+policy+CHECK 3 รูป · ไม่เปิด browse ก่อน login (pete ตอบแล้ว) · อ่าน PT-09/10/12 ก่อนเริ่ม |
| 3 | Browse / Search / Filter | 🟨 **RPC `search_products()` แทน `shuffle_key`/exact-match filter เดิมแล้ว (D-62, ปิดข้อเสนอ P-05)** — substring title+description, category, price range ในตัวเดียว · trigram index เดิมยังใช้อยู่ (D-46) · **เพิ่ม `advertisement_posts`/`advertisement_likes`/view (D-58)** | 🟨 `AllList`+`ProductDetails` ทำแล้ว · category filter chip requery จริงแล้ว (D-37) · `AllList` เป็น grid 2 คอลัมน์ + รูปจริงแล้ว (D-38) · `ProductDetails` โชว์รูปจริง + fallback icon แล้ว (D-42) · **ค้นหาเป็น substring จริงแล้ว + ช่วงราคาแล้ว (D-62/D-63)** ผ่าน RPC ทุก entry point (onLoad/search/ปุ่ม/pull-to-refresh/13 category chip) — ยังไม่ทดสอบผ่านแอปจริง · `ListView_6etuspo6` โชว์ประกาศแอดมิน + ไลก์จริงแล้ว (D-58) ยังไม่ทดสอบผ่านแอปจริง | `Home` grid สินค้าไม่มีรูปยัง crash ได้ (`firstImageUrl!` ไม่มี fallback) · ยังไม่มี empty-state UI (ลองแล้วไม่สำเร็จ D-46, ยังไม่ลองรอบ 3 หลัง D-62) |
| 4 | Chat & Messaging | ✅ RLS membership-based + RPC/trigger ทดสอบสิทธิ์จริงผ่านแล้ว (D-29/D-30) | 🟨 3 ทางเข้า + Drawer nav, ข้อความ+รูป+bubble UI ใช้ได้จริง **ทดสอบผ่านแอปจริงแล้ว (D-40/D-41)** · ปุ่มแชท/รายงานซ่อนจากเจ้าของประกาศเองแล้ว (D-51) · **Realtime ทำแล้วทั้ง `chatList`/`chatMessages` + ปุ่ม "แชทกับผู้ขาย" ส่ง memberNames/userIds แล้ว (D-60)** | จุดแดง unread stale-state แก้แล้ว (D-49) · D-49/D-51/D-60 ยังไม่ทดสอบผ่านแอปจริง · อ่าน PT-06/09/22/23/24/32 ก่อนแตะต่อ |
| 5 | Transaction & Status | 🟨 `transactions`+RLS+RPC `mark_product_sold`+trigger `enforce_sale_via_rpc_only` ครบ (D-59) | 🟨 `chatMessages`/`Home`/`Mypost`/`ProductDetails` ผูกครบ ยังไม่ทดสอบผ่านแอปจริง (D-59) | ปิดหนี้ D-03 ไปพร้อมกัน (ดูแถว L8) |
| 6 | Notifications | 🟨 ตาราง+RLS apply แล้ว · **เพิ่มเข้า `supabase_realtime` publication แล้ว (D-60)** | 🟨 `Notifications` page + bell icon + link ไป `ProductDetails` (D-26) + จุดแดง unread (D-31) stale-state แก้แล้ว (D-49) · **รายการอัปเดตสดระหว่างเปิดหน้าค้างอยู่แล้ว (D-60)** ยังไม่ทดสอบผ่านแอปจริง | เขียนได้ทางเดียว: reject→insert · ไม่มี unread badge ตัวเลข/push จริงตอนปิดแอป |
| 7 | Reviews & Reports | 🟨 `reports` RLS+constraint เสร็จ (D-24) + `is_read` (D-31) + **`reported_user_id`+2 CHECK+unique index ครบแล้ว (D-65, ปิดข้อเสนอ P-09)** · **`reviews` table+RLS+`products_review_view` 4 คอลัมน์ใหม่ ครบแล้ว (D-64)** | 🟨 `ReportProductSheet`/`ReportsFeedback`/`ReportDetail` + จุดแดง unread (D-31) stale-state แก้แล้ว (D-49) ยังไม่ทดสอบผ่านแอปจริง — เนื้อหาหลัก**ทดสอบผ่านแอปจริงแล้ว (pete, 2026-08-15)** · **`RateSellerSheet`+ปุ่ม "ให้คะแนนผู้ขาย" บน `ProductDetails` ครบแล้ว (D-64) ยังไม่ทดสอบผ่านแอปจริง** · **`ReportUserSheet`+ปุ่ม "รายงานผู้ใช้" ใน `UserProfileCard`+`ReportDetail` แยกโชว์ประเภทรายงาน ครบแล้ว (D-65) ยังไม่ทดสอบผ่านแอปจริง** | หน้า "Reports" ถูก pete rename เป็น "ReportsFeedback" ตรงใน editor (2026-08-17) — ตรวจแล้วไม่มีจุดอ้างชื่อเก่าค้าง |
| 8 | Admin Dashboard | 🟨 เริ่มแล้ว 2026-08-14 · `admin_sales_by_seller` gate ปิดแล้ว (D-33) **+ repoint ไปอ้าง `transactions` แล้ว (D-59)** · **ระบบ ban ปิดครบแล้ว (D-52)** · **`ad-post-images` bucket + policy (D-58)** | 🟨 `HomeAdmin` ผูกข้อมูลจริง + approve/reject ใช้งานได้ · **หน้า `ManageUsers` + `BanUserSheet` + popup แจ้งผู้ถูกแบน ทำครบแล้ว (D-52) ยังไม่ทดสอบผ่านแอปจริง** · **หน้าใหม่ `AdminCreatePost` (โพสต์ประกาศ/แบนเนอร์) + nav entry บน side-nav (D-58) ยังไม่ทดสอบผ่านแอปจริง** · **`SalesBySellerList` ได้ข้อมูลจริงจาก `transactions` แล้ว (D-59) ไม่ต้องแก้ DSL** | trigger คุ้มกัน moderation 2 คอลัมน์ (D-23) + ban 4 คอลัมน์ (D-52) + sale `status`/`buyer_id` (D-59) · 5 RESTRICTIVE policy กันผู้ถูกแบน (D-52) · ~~เหลือ RLS admin-only เต็มรูปแบบ (`products` คอลัมน์อื่น)~~ **ปิดแล้ว (D-59, D-03 debt เดิม)** · `"CAT"` CRUD ยังผ่าน UI ไม่ได้ |

✅ เสร็จ · 🟨 กำลังทำ · ⬜ ยังไม่เริ่ม — คอลัมน์ FlutterFlow คือสถานะใน **v2** (`m-j-u-market-v2-0xhjhg`) เท่านั้น งานฝั่ง v1 (archived) ไม่นับ (D-16)

> 🔴 **กฎการให้ ✅:** "ตารางว่าง 0 แถว = ยังไม่ PASS" — นับเป็น ✅ ได้ต่อเมื่อทดสอบด้วย user ธรรมดาจริงแล้วเท่านั้น

**L1** ปิดฝั่ง Supabase แล้ว (2026-08-09) · FlutterFlow ค้างที่ Confirm Email — ลิงก์ (D-19) ใช้ไม่ได้เพราะ Microsoft Safe Links ดึง token ทิ้งก่อนคนกด → เปลี่ยนเป็น OTP (D-20) แต่อีเมลไปไม่ถึงกล่อง (deliverability ฝั่ง tenant) รายละเอียด `DECISIONS.md` **D-20** · 🔴 พบ 2 บัญชีจริงที่ `handle_new_user()` ไม่ทำงาน (D-32) ยังไม่ปิดสนิท

**L2** ปิดไม่ได้เพราะยังไม่เคยเทสอัปไฟล์เกิน 5MB/ผิดชนิดผ่านแอปจริง (บังคับที่ Storage API ไม่ใช่ Postgres ทดสอบจาก DB แทนไม่ได้) — อัปสำเร็จ + path ถูกต้องยืนยันแล้ว (`VERIFICATION.md` V-08)

**L4** Supabase ปิดแล้ว (D-29, 2026-08-16) — `chat_summary.member_names` พิสูจน์แล้วว่าไม่เป็น NULL ด้วย user ธรรมดาจริง (มีห้องแชท 1 ห้อง 2 สมาชิกจากการทดสอบ) FlutterFlow ปิดไม่ได้เพราะยังไม่มี Realtime — ข้อความ/รูป/bubble UI ทดสอบผ่านแอปจริงแล้ว (D-40/D-41) รายละเอียด `layers/L4-chat.md`

---

## ❓ คำถามที่ยังไม่ตัดสินใจ (รวมทุก layer)

**🔴 รอ pete ตอบ**
1. **D-20 — OTP เสร็จ ติด email deliverability** ดู `DECISIONS.md` **D-20** — ไม่ใช่คำถามค้าง เป็นบล็อกทางเทคนิค
2. **รับข้อเสนอ P-11/P-12 ไหม** — unique index บน `lower(email)` + เก็บกวาดไฟล์กำพร้า ยังไม่ตอบรับ อยู่ใน `PROPOSED_SQL.md`

**Layer 1** — role-based redirect ที่ Splash/Initial (auto-login) ด้วยไหม · `handle_new_user()` ยังไม่มี `ON CONFLICT`

**Layer 2** — ตอบแล้วทั้งคู่ (pete, 2026-08-24, D-61): ไม่เปิด browse ก่อนล็อกอิน (คงเดิม) · บังคับ `category_id` ห้าม null แล้ว — ไม่มีคำถามค้าง

**Layer 3** — กลับคำ D-45 แล้ว (D-62, 2026-08-24): สร้าง RPC `search_products` จริงตามที่ pete สั่ง ปิดข้อเสนอ P-05 · ช่วงราคาทำแล้วด้วย (D-63) — คำถามที่เหลือ: จะลงทุนทำ empty-state UI ต่อไหม (ลองแล้วไม่สำเร็จ 2 วิธี D-46, ยังไม่ลองรอบ 3 หลัง RPC เปิดทางใหม่)

**Layer 4** — ตอบแล้วทั้งหมด (D-29): realtime ต้องฟังที่ table `chat_message` ไม่ใช่ view — **ต่อจริงแล้ว (D-60)**, ไม่ต้อง array-contains เพราะ RLS กรองให้แล้ว, trigger auto-update `last_message` apply แล้ว — ปุ่ม "แชทกับผู้ขาย" ส่ง `memberNames`/`userIds` แล้ว (D-60) — ไม่มีคำถามค้าง

**Layer 5** — ตอบครบแล้ว (D-59): `available`/`reserved`/`sold` พอ (`reserved` เผื่ออนาคต ยังไม่มี UI ขับ), เก็บประวัติแยกจริงในตาราง `transactions` ไม่ใช้แค่ `products.status` — ไม่มีคำถามค้าง

**Layer 6** — realtime บนรายการทำแล้ว (D-60) — ยังไม่ทำ: notification จาก `chat_message` insert (P-04, ตัดสโคปไว้ตอน D-60 รอ pete ตัดสินใจ), unread badge ตัวเลข

**Layer 7** — `reviews` ตอบครบแล้ว (D-64): public read + immutable + ผูก `transactions` · รีพอร์ตผู้ใช้ตอบครบแล้ว (D-65, ปิดข้อเสนอ P-09) — ไม่มีคำถามค้างของ L7 แล้ว

**Layer 8** — ~~RLS admin-only เต็มรูปแบบยังไม่ทำ (`products` ยัง allow-all, D-03)~~ **ปิดแล้ว (D-59)** — `chat`/`chat_user`/`chat_message` ปิดหนี้นี้ตั้งแต่ D-29 · หน้า `Inspect` แยกยังไม่มีใน v2 (คิวรอตรวจอยู่ใน `HomeAdmin` มีปุ่มอนุมัติ/ปฏิเสธจริงแล้ว) · `"CAT"` ยังจัดการผ่าน UI ไม่ได้ ต้อง seed ด้วยมือ · ~~`admin_sales_by_seller` อ้าง `products.status='sold'` ชั่วคราว~~ **repoint ไปอ้าง `transactions` แล้ว (D-59)**

---

## 💣 หนี้ทางเทคนิคที่ต้องใช้คืนก่อน production

- [x] ~~**[D-32] 2 บัญชี `auth.users` ไม่มีแถวใน `"Profile"`** (`mju6606105382`/`mju6606105386`)~~ **ปิดแล้วทั้งคู่** — `mju6606105382` แก้ด้วยลบบัญชีเก่า+สมัครใหม่สะอาด (D-34, 2026-08-17) `mju6606105386` ลบทิ้งเป็นบัญชีทดสอบค้าง (pete ยืนยัน 2026-08-24) root cause เดิมของ D-32 ยังไม่ทราบแน่ชัด แต่ไม่เกิดซ้ำในรอบทดสอบล่าสุด — เฝ้าดูต่อถ้าเกิดซ้ำกับบัญชีใหม่
- [x] ~~`products` เป็น allow-all RLS นอกเหนือ 2 คอลัมน์ moderation — non-admin ยัง `UPDATE`/`DELETE` คอลัมน์อื่น (ราคา/รูป/ชื่อ) ของสินค้าคนอื่นได้~~ **ปิดแล้ว 2026-08-23 (D-59)** — allow-all policy ถอดออกแล้ว แทนด้วย 4 policy ตาม cmd จริง (`SELECT` เปิดกว้างเหมือนเดิม, `INSERT`/`UPDATE`/`DELETE` จำกัด owner-or-admin) trigger `enforce_moderation_admin_only` (D-23) + `enforce_sale_via_rpc_only` (D-59) ยังทำงานถูกทับได้ตามปกติ (`chat`/`chat_user`/`chat_message` ปิดหนี้เดียวกันนี้ตั้งแต่ D-29) — ยืนยัน impersonation test จริง (non-owner บล็อก, owner แก้ฟิลด์ปกติได้แต่แก้ `status` ตรงไม่ได้)
- [ ] ไม่มีระบบกันกดปุ่มลบซ้ำ/popup ค้างใน reject flow (ยอมรับเป็น MVP)
- [ ] `"Profile".id` มี default `gen_random_uuid()` ทั้งที่เป็นคอลัมน์ FK — ควรถอด default ออก (ยังไม่ทำ)
- [ ] ข้อมูลทดสอบค้างใน DB: `"Profile"` มี 7 แถว — 4 แถวมี `full_name` ปลอม (`ทดสอบ นักศึกษาหนึ่ง/สอง/สาม`, `สมชาย ใจดี`) ต้องล้างก่อน production
- [ ] 🆕 **พบบัญชี admin `mju6500000001@mju.ac.th` ที่ไม่มีบันทึกที่มา** (ตรวจพบ 2026-08-15 ตอน sync เอกสาร) — `created_at` = `email_confirmed_at` เป๊ะ (ไม่ผ่าน OTP flow จริง เพราะ confirm-email ยังไม่เคยทำงาน) ไม่อยู่ในตารางบัญชีทดสอบด้านล่าง — ต้องหาว่าใครสร้าง/ทำไม แล้วบันทึกหรือลบทิ้ง
- [ ] `"CAT"` ไม่มี UNIQUE บน `name` — seed ซ้ำได้
- [x] ~~`"Profile".is_banned` ไม่มี enforcement~~ **ปิดแล้ว 2026-08-21 (D-52)** — soft ban บังคับที่ RLS จริงแล้ว (ลงประกาศ/แชท/รายงานไม่ได้ · ปลดแบนตัวเองไม่ได้ · ประกาศถูกซ่อนจากคนอื่น) UI ครบแล้วด้วย (Phase B–D: `ManageUsers`/`BanUserSheet`/popup บน Home/ปุ่มใน `ReportDetail`) — เหลือแค่ pete ทดสอบผ่านแอปจริง
- [ ] ไฟล์กำพร้าใน `avatars`/`product-images` — เปลี่ยน/ลบแล้วไฟล์เก่าไม่ถูกลบ (D-15) ยังไม่มีระบบเก็บกวาด (P-12 ยังไม่เลือกแนวทาง)
- [ ] รูปของประกาศ `pending`/`rejected` เปิดดูได้ถ้ารู้ URL (public bucket, D-12) — ห้ามเก็บของอ่อนไหวในนี้
- [ ] `Profile_email_key` เป็น unique ธรรมดา ไม่ใช่ index บน `lower(email)` — ยังไม่มีปัญหาจริงเพราะ trigger `lower()` ให้ก่อน insert (P-11 ยังไม่ตอบรับ)
- [ ] repo private (D-13) ไม่ใช่การปิดช่องโหว่ — ของจริงที่ต้องปิดคือ 2 ข้อบนสุดของรายการนี้

**บัญชีทดสอบที่ credential ถูกแก้ตรงด้วย SQL (ไม่ใช่สภาพ user จริง — ห้ามใช้เทสอย่างอื่นโดยไม่รู้ตัว):**

| อีเมล | role | แก้อะไร | ใช้เทสอะไรได้ | ห้ามเทส |
|---|---|---|---|---|
| `mju6577778888@mju.ac.th` | admin | `email_confirmed_at` patch ด้วย SQL | auth/role routing, ทุก layer อื่น | "สมัครแล้วต้องยืนยันอีเมล" |
| `mju6512345678@mju.ac.th` | user | `encrypted_password` เขียนทับด้วย SQL | Test Pilot login | เหมือนกัน |
| `mju6500000002@mju.ac.th` (สร้าง 2026-08-15) | user | insert ตรงเข้า `auth.users`+`auth.identities` (PT-20) | L7 report-a-listing ด้วย non-admin | "สมัครแล้วต้องยืนยันอีเมล" |

🔴 **บัญชีทดสอบสดสำหรับ confirm-email เดิมถูกลบไปแล้ว 2026-08-10** (`mju6500000099`+อีก 3 บัญชี) — ไม่มีบัญชีสดพร้อมใช้ ต้องสมัครใหม่เมื่อกลับมาทำ D-20 ต่อ

---

## 📋 แม่แบบเปิด session กับ Claude Code

```
[git pull ก่อนเสมอ]
Layer ที่ทำอยู่: [เช่น Layer 1 — trigger auto-insert Profile]
อ่านก่อน: CLAUDE.md → docs/STATUS.md → [ไฟล์ตามตาราง router ใน CLAUDE.md]
Supabase objects ที่เกี่ยวข้อง: [ชื่อ table/column/FK จริง — ดู docs/SCHEMA.md ห้ามพิมพ์จากความจำ]
FlutterFlow page/state ที่เกี่ยวข้อง: [ชื่อ Page, Page/App State, widget parameter]
สิ่งที่ต้องการ: [ระบุให้ชัด]
Error / screenshot Action Flow (ถ้ามี): [แนบ]
```

> ถ้ามี `flutterflow ai` (MCP) ให้สั่งใช้ `inspect`/`search`/`status` ดึงชื่อจริงมาก่อน แทนพิมพ์เอง

---

## 🔚 เช็คลิสต์ปิด session (ห้ามข้าม)

- [ ] `INBOX.md` ว่างแล้ว (ของที่ pete เขียนมาถูกกระจายเข้าที่หมดแล้ว)
- [ ] `SCHEMA.md` ตรงกับ DB จริง (สั่ง `db-verifier` ตรวจให้ก็ได้)
- [ ] SQL ที่ apply ไปแล้ว ย้ายออกจาก `PROPOSED_SQL.md` เข้า `SCHEMA.md` แล้ว
- [ ] ตาราง "สถานะ 8 Layers" ด้านบนอัปเดตแล้ว
- [ ] "คิวถัดไป" อัปเดตแล้ว
- [ ] ตัดสินใจใหม่ (ถ้ามี) บันทึกลง `DECISIONS.md` แล้ว
- [ ] doc-syncer รายงานว่าไม่มีไฟล์ไหนเกินเพดาน (หรือรับทราบที่เกินแล้ว)
- [ ] **`git add -A && git commit && git push`** — ไม่ push = เครื่องอื่นทำงานกับข้อมูลเก่า
